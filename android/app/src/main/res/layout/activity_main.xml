<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/quip_bg">

    <!-- Slim status bar: connection indicator + settings entry point. Everything else
         (host IP, connect button, layout picker) now lives in SettingsActivity so the
         keyboard and trackpad below get as much vertical room as possible. -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:paddingStart="14dp"
        android:paddingEnd="4dp"
        android:paddingTop="5dp"
        android:paddingBottom="5dp"
        android:background="@color/quip_bg_elevated">

        <View
            android:id="@+id/statusDot"
            android:layout_width="9dp"
            android:layout_height="9dp"
            android:background="@drawable/bg_status_dot" />

        <TextView
            android:id="@+id/tvStatus"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="8dp"
            android:text="Not connected"
            android:textColor="@color/quip_text_secondary"
            android:textSize="12sp" />

        <TextView
            android:id="@+id/btnSettings"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:text="\u2699"
            android:textSize="18sp"
            android:textColor="@color/quip_text_primary"
            android:gravity="center"
            android:clickable="true"
            android:focusable="true"
            android:background="?attr/selectableItemBackgroundBorderless" />

    </LinearLayout>

    <!-- Filled at runtime: either the classic Keyboard|Trackpad panel (grid key-layouts)
         or a freeform HUD (control-scheme layouts, e.g. joystick + buttons + background
         trackpad) — see MainActivity.renderSchemeIfNeeded(). -->
    <FrameLayout
        android:id="@+id/contentContainer"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1" />

</LinearLayout>
